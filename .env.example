# Optional. The complete synthetic demo works without an API key.
OPENAI_API_KEY=

# Build Week requires meaningful GPT-5.6 use. Keep this explicit and verify
# model access in the deployment account before enabling Live Analysis.
OPENAI_MODEL=gpt-5.6
